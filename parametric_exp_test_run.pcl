# try the 4-interval regular condition first
int this_interval_cond = 1;

# create the list of stimuli of all trials in this block
array <int> this_index_of_whole_block[0];
loop int i = 1 until i > (setting_interval_in_block / this_interval_cond) begin # the sequence will repeat for [480 / 4 intervals] times
	this_index_of_whole_block.append(order_1intervals);
	i = i+1;
end;

array <int> stimList[][] = assignDeviant(this_interval_cond, this_index_of_whole_block);
term.print_line(stimList[1]);
term.print_line(stimList[2]);
